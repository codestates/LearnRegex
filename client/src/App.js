import React from 'react';

// 확인을 위해 넣어둠 - 삭제해도 됨
// import QuizListElement from './components/QuizListElement';

function App() {
  return (
    <div>
      HelloWorld
      {/* 확인을 위해 넣어둠 - 삭제해도 됨
      <QuizListElement id={1} title={'비밀번호 유효성 검사'} nickname={'김코딩'} count={3} isClear={true} /> */}
    </div>
  );
}

export default App;
